/**
 * Configuration types for complexity routing (re-export for config system).
 */

export type {
  TaskComplexity,
  ComplexityRoutingStrategy,
  CostControlConfig,
  ClassificationThresholds,
  ComplexityRoutingConfig,
} from "../agents/complexity-routing.types.js";
